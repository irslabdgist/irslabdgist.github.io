source "https://rubygems.org"

# Pin to GitHub Pages' Jekyll/plugin versions so local preview matches production.
# https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins

# Required on Ruby 3.0+ (no longer in stdlib).
gem "webrick"
